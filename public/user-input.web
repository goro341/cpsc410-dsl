CREATE PAGE 1
CREATE HEADER h
ADD "ldsflk" to h
ADD h to 1
CREATE GRID g
ADD g to 1
CREATE MD BLOCK one
ADD "hello" to one
ADD one to g

LOAD "/test.md" with robots
ADD robots to 1

CREATE MD BLOCK two
ADD "hello world" to two
ADD two to g
BUILD
